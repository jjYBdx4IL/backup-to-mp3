# STATUS

Survives the mp3 compression, but not the convolution.

Param defaults are optimized for MP3 compression and 48 kHz sampling rate.

One thing to think about is the cycle period. Probably not optimal for mp3.
