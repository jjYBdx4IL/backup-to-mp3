diff --git a/amodem/__main__.py b/amodem/__main__.py
index 70227ba..ff4dea2 100644
--- a/amodem/__main__.py
+++ b/amodem/__main__.py
@@ -12,6 +12,7 @@ import pkg_resources
 from . import async_reader
 from . import audio
 from . import calib
+from . import common
 from . import main
 from .config import bitrates
 
@@ -151,6 +152,9 @@ def create_parser(description, interface_factory):
     receiver.add_argument(
         '--plot', action='store_true', default=False,
         help='plot results using pylab module')
+    receiver.add_argument(
+        '--ignore-checksum-errors', action='store_true', default=False,
+        help='Ignore checksum errors')
     receiver.set_defaults(
         main=lambda config, args: main.recv(
             config, src=args.src, dst=wrap(Decompressor, args.dst, args.zlib),
@@ -222,6 +226,9 @@ def _main():
     args = p.parse_args()
     _config_log(args)
 
+    if hasattr(args, 'ignore_checksum_errors'):
+        common.IGNORE_CHECKSUM_ERRORS = args.ignore_checksum_errors
+
     # Parsing and execution
     log.info(description)
 
@@ -256,6 +263,9 @@ def _main():
             args.dst.close()
             log.debug('Finished I/O')
 
+    if common.checksum_error_count:
+        sys.exit(88)
+
 
 if __name__ == '__main__':
     _main()
diff --git a/amodem/common.py b/amodem/common.py
index 0aba546..fb302f0 100644
--- a/amodem/common.py
+++ b/amodem/common.py
@@ -11,7 +11,8 @@ import numpy as np
 log = logging.getLogger(__name__)
 
 scaling = 32000.0  # out of 2**15
-
+IGNORE_CHECKSUM_ERRORS = False
+checksum_error_count = 0
 
 def load(fileobj):
     """ Load signal from file object. """
diff --git a/amodem/framing.py b/amodem/framing.py
index 946e4b9..7dc6c80 100644
--- a/amodem/framing.py
+++ b/amodem/framing.py
@@ -27,7 +27,9 @@ class Checksum:
         expected = _checksum_func(payload)
         if received != expected:
             log.warning('Invalid checksum: %08x != %08x', received, expected)
-            raise ValueError('Invalid checksum')
+            common.checksum_error_count += 1
+            if not common.IGNORE_CHECKSUM_ERRORS:
+                raise ValueError('Invalid checksum')
         log.debug('Good checksum: %08x', received)
         return payload
 
diff --git a/pyproject.toml b/pyproject.toml
index 1011fd6..ecf8cb0 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -9,7 +9,8 @@ readme = "README.md"
 license = {text = "MIT"}
 requires-python = ">=3.7"
 dependencies = [
-  "numpy",
+  "numpy<2",
+  "setuptools<80"
 ]
 classifiers = [
     "Development Status :: 5 - Production/Stable",
